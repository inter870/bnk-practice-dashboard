# Streamlit 금융 대시보드 PRD

## 1. 목적
주식 투자자가 오늘의 시장 상태를 빠르게 해석하고, 종목별 행동 기준을 정리하며, 공시와 거시지표까지 한 화면에서 확인할 수 있는 금융 대시보드를 제공한다.

이 앱의 핵심 목표는 다음과 같다.
- 코스피, 코스닥, 환율, 금리, 공포·탐욕, ECOS 핵심 매크로를 한눈에 보여준다.
- 사용자가 입력한 최대 15개 종목의 당일 흐름과 최근 60일 추세를 비교한다.
- 공시를 호재/중립/악재로 자동 분류한다.
- GPT 시장 브리핑으로 오늘의 시장을 구조화된 문장으로 요약한다.

## 2. 대상 사용자
- 국내 주식 중심으로 매매하는 개인 투자자
- 시장 레짐과 업종 방향성을 함께 보고 싶은 투자자
- 주요 종목의 단기 수급과 공시를 빠르게 확인하고 싶은 사용자

## 3. 제품 범위

### 포함 범위
- Streamlit 단일 앱 `app.py`
- FinanceDataReader 기반 시세 및 지표 조회
- DART 기반 공시 조회
- Alternative.me 공포·탐욕 지수 조회
- 한국은행 ECOS 핵심 매크로 조회
- GPT 브리핑 생성 및 검증
- 브리핑 파일 저장

### 제외 범위
- 실거래 주문 기능
- 계좌 연동
- 자동매매
- 유료 데이터 API
- 사용자 로그인/권한 시스템

## 4. 사용자 경험 원칙
- 첫 화면에서 오늘 시장 상태를 바로 읽을 수 있어야 한다.
- 숫자는 많아도 해석은 짧고 명확해야 한다.
- 실패한 데이터 소스가 있어도 앱 전체가 죽지 않아야 한다.
- 잘못된 값은 숨기지 말고 N/A, 경고, 대체값으로 보여준다.
- 시각적 색상 규칙은 일관되게 유지한다.
  - 상승: 빨강
  - 하락: 파랑
  - 보합/중립: 회색

## 5. 화면 구조

### 5.1 상단 영역
상단에는 다음 카드형 지표를 표시한다.
- 코스피
- 코스닥
- USD/KRW
- 미국 국채 10년물 금리
- 한국 국채 3년물 금리 또는 대체 금리
- ECOS 핵심 매크로 카드
- 공포·탐욕 지수 카드 또는 바 차트

각 카드에는 다음 정보를 포함한다.
- 현재값
- 전일 대비 변화
- 전일 대비 변동률
- 데이터 출처
- 마지막 조회 시각

### 5.2 중단 영역
사용자가 입력한 최대 15개 종목에 대해:
- 당일 수익률 비교 차트
- 종목명과 종목코드
- 전일 대비 수익률 수치
- 클릭 가능한 상세 선택 UI

### 5.3 하단 영역
선택한 종목에 대해:
- 최근 60거래일 캔들 차트
- 거래량 바 차트
- 5일, 20일, 60일 추세 요약
- 상대강도 요약
- 수급/변동성 요약

### 5.4 공시 탭
별도 탭에서:
- 공시 목록 표시
- 회사명/보고서명 검색
- 공시 자동 분류: 호재 / 중립 / 악재
- 결과 없음 처리
- 외부 링크 이동

### 5.5 GPT 시장 브리핑 섹션
대시보드 하단에 추가된다.
- 버튼 클릭 시 브리핑 생성
- 로딩 중 `st.spinner("분석 중...")` 표시
- 시스템 프롬프트와 지식 파일을 읽어 GPT에 주입
- 출력 검증 후 화면 노출
- 결과 파일 저장

## 6. 데이터 소스

### 6.1 FinanceDataReader
주요 데이터 원천이다.
- 지수
- 환율
- 국채 금리
- 종목 OHLCV
- 종목 상장 목록

### 6.2 DART
공시 목록과 공시 분류에 사용한다.

### 6.3 Alternative.me
공포·탐욕 지수에 사용한다.

### 6.4 한국은행 ECOS
핵심 거시지표 해석에 사용한다.
- 기준금리
- GDP
- CPI
- M2
- 무역수지

### 6.5 OpenAI API
GPT 시장 브리핑 생성에 사용한다.
- 모델: `gpt-5.5`
- 키는 `.env`의 `OPENAI_API_KEY`에서 읽는다.

## 7. 핵심 기능 요구사항

### R1. 상단 카드 자동 로드
- 앱 실행 시 오늘 기준 데이터를 자동 불러온다.
- 코스피, 코스닥, USD/KRW, 미국 10년물, 한국 3년물, ECOS 핵심 지표를 표시한다.
- 전일 대비 등락을 색상으로 구분한다.
- 휴장일 또는 데이터 미수신 시 가장 최근 거래일로 폴백한다.

### R2. 종목 입력 및 중단 차트
- 사이드바에서 종목 코드를 최대 15개 입력할 수 있어야 한다.
- 쉼표, 줄바꿈 모두 허용한다.
- 잘못된 코드는 별도 경고로 표시한다.
- 중단에서 당일 수익률 비교 차트를 표시한다.

### R3. 상세 차트
- 중단에서 종목을 클릭하면 하단 상세 차트가 바뀌어야 한다.
- 최근 60거래일 캔들과 거래량이 표시되어야 한다.

### R4. 출처 및 조회 시각
- 모든 데이터 하단에 `출처: FinanceDataReader`를 표시한다.
- 마지막 조회 시각을 함께 표시한다.
- 새로고침 버튼으로 재조회가 가능해야 한다.

### R5. 공시 탭
- 공시 목록이 실제로 표시되어야 한다.
- 회사명 또는 보고서명으로 검색할 수 있어야 한다.
- 검색 버튼이 있어야 한다.
- 공시가 0건이면 빈 화면 대신 안내 문구를 보여줘야 한다.
- 공시를 호재/중립/악재로 자동 분류해야 한다.

### R6. GPT 시장 브리핑
- 브리핑 생성 버튼으로 실행한다.
- `system_prompt_briefing.md`, `kb_market_indicators.md`, `kb_sector_mapping.md`, `kb_signal_rules.md`를 순서대로 시스템 입력에 주입한다.
- 현재 시장 데이터와 주요 공시를 사용자 입력으로 만든다.
- `output_schema.md` 기준으로 출력 형식을 검증한다.
- 금지 표현:
  - 매수 추천
  - 목표가
  - 보장
- 면책 문구가 없으면 코드에서 강제로 추가한다.
- 검증 실패 시 경고 표시와 재생성 흐름을 제공한다.
- 검증 통과 시 `st.info()`로 표시하고 파일로 저장한다.

### R7. 시장 해석 및 메탈 코치
- 시장 레짐, 추세, 상대강도, 거래량, 금리, 환율, 공포·탐욕을 종합해 해석한다.
- 결과는 매수/중립/매도 형태의 라벨로 제공한다.
- 오늘의 시장에 맞는 행동 원칙을 짧고 강하게 제시한다.

## 8. 판단 로직

### 8.1 시장 레짐
- 코스피와 코스닥의 방향성
- USD/KRW 변화
- 미국 10년물 금리 변화
- 한국 3년물 금리 변화
- 공포·탐욕 지수 레벨
- ECOS 핵심 거시지표의 압력

### 8.2 종목 점수
종목별 점수는 다음 요소를 조합한다.
- 5일, 20일, 60일 수익률
- 5일/20일/60일 이동평균 대비 위치
- 최근 거래량 확인
- 코스피 대비 상대강도
- 최근 변동성
- 시장 레짐 보정치

### 8.3 시그널 라벨
- 고득점: 매수
- 중간: 중립
- 저득점: 매도

이 라벨은 감정이 아니라 규칙 기반 점수의 결과여야 한다.

## 9. GPT 브리핑 요구사항

### 9.1 입력 데이터 형식
브리핑 입력은 아래 항목을 반드시 포함한다.
- 코스피
- 코스닥
- 원달러 환율
- 공포·탐욕 지수
- 기준금리
- 국고채 3년
- 조회 기준일
- 주요 공시
- 데이터 미수신 항목

### 9.2 출력 검증
다음 조건을 검증한다.
- 섹션 헤더 5개 이상
- 금지 표현 없음
- 면책 문구 포함

### 9.3 저장
- 저장 경로: `briefings/briefing_YYYYMMDD.txt`
- UTF-8로 저장

## 10. 비기능 요구사항

### 성능
- 주요 데이터는 캐싱한다.
- 앱 초기 화면은 빠르게 떠야 한다.
- 브리핑 생성은 네트워크 실패 시에도 앱 전체를 멈추지 않아야 한다.

### 안정성
- 외부 API 실패 시 빈 화면이 아니라 메시지를 보여준다.
- 일부 시계열이 비어 있어도 대체 경로로 렌더링한다.

### 유지보수성
- 데이터 로더, 시그널 엔진, 렌더링, GPT 브리핑은 분리된 함수로 유지한다.
- 프롬프트 파일은 코드와 분리하여 관리한다.

### 국제화/문자
- 한글이 깨지지 않도록 폰트 설정을 유지한다.
- 화면 문자열은 UTF-8 기준으로 관리한다.

## 11. 파일 구조

### 핵심 파일
- `app.py`
- `requirements.txt`
- `PRD.md`
- `output_schema.md`
- `system_prompt_briefing.md`
- `kb_market_indicators.md`
- `kb_sector_mapping.md`
- `kb_signal_rules.md`

### 런타임 생성 파일
- `briefings/briefing_YYYYMMDD.txt`

## 12. 환경 변수
- `DART_API_KEY`
- `ECOS_API_KEY`
- `OPENAI_API_KEY`

API 키는 코드에 직접 쓰지 않는다.

## 13. 수용 기준
다음 조건을 만족해야 한다.
- 앱 실행 시 상단 카드가 자동 표시된다.
- 사용자 입력 종목이 중단 차트에 반영된다.
- 종목 클릭 시 하단 상세 차트가 바뀐다.
- 공시 탭에서 검색과 분류가 정상 동작한다.
- 공포·탐욕 지수가 카드 또는 차트로 표시된다.
- ECOS 핵심 지표가 N/A가 아닌 실제 값으로 연결된다.
- GPT 브리핑이 생성, 검증, 저장까지 동작한다.
- 브리핑 실패 시 경고가 보이고 앱이 멈추지 않는다.

## 14. 리스크 및 대응
- 외부 API 제한 또는 장애
  - 캐시와 폴백 메시지로 대응한다.
- 인코딩 문제
  - UTF-8 저장과 한글 폰트 설정으로 대응한다.
- GPT 출력 품질 저하
  - 시스템 프롬프트와 출력 검증으로 대응한다.
- 지표 해석 오판
  - 자동 시그널은 참고용으로 표시하고 면책 문구를 강제한다.

## 15. 향후 확장
- 종목군별 섹터 모듈 강화
- 사용자 맞춤 watchlist 저장
- 브리핑 히스토리 뷰
- 장중/장마감 모드 분리
- 멀티 모델 브리핑 비교

